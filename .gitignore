# dependencies
node_modules/

# next build output
.next/
out/

# environment secrets
.env
.env.local
.env.production
.env.test.local

# exports
exports/

# local sqlite leftovers / backups
prisma/*.db
prisma/*.db-journal
prisma/dev.db*

# claude local config
.claude/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# OS junk
.DS_Store
Thumbs.db

# cloud-sync / browser-download duplicates ("schema (1).prisma" etc.)
# These have silently clobbered tracked files — never commit them.
*\ \(1\).*
*\ \(2\).*
prisma/prisma/

# uploaded tenant assets
public/uploads/*
!public/uploads/.gitkeep